# Data-Logger-
Arduino-based data logger that reads temperature and humidity from a DHT22 sensor every 60 seconds and stores the data in CSV format on an SD card. Uses non-blocking timing (millis()), includes error handling, and outputs logs to Serial Monitor for debugging.
